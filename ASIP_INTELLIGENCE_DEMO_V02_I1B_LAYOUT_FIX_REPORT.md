# I1-B 图谱布局与交互可点击性修正报告

- 任务：I1-B（小型修正包）
- 日期：2026-08-06
- 分支：`feature/asip-intelligence-v02`（I1-B 提交追加于 I1-A 之后）
- 范围：仅修正关系图布局与可点击性；不扩库、不新增功能、不改实体/关系事实。

## 1. 修正的问题

1. **节点过近、标签压线**：同圈层节点最小中心距不足（旧布局仅 58px 碰撞下限），节点标题与关系标签可能压住箭头和连线，箭头难以识别、关系线难以准确点击。
2. **同圈层节点共用同一条射线**：旧布局按实体类型固定基准角，同类型并列节点被分配到几乎相同的极角，视觉上像串联/上下级关系，而非并列围绕中心。

## 2. 布局算法调整（`assets/js/intelligence/network.js`）

### 2.1 同圈层最小间距

- 桌面端最小中心距 `minSpacing = 100px`（规格 90–110px 区间），窄屏（画布 <560px）降至 84px；
- 同圈层节点先按"均匀展开 + 类型错位角"分配角度；
- 圈层半径自适应：当同圈节点数导致弧长小于最小间距时，按 `radius = minSpacing*(n-1)/π` 迭代扩大半径（每圈额外 +14px 余量）；
- 全局碰撞避免：跨圈层任意节点对最小距离 100px（窄屏 84px），推挤迭代从 2 轮增至 4 轮。

### 2.2 同圈层角度分布

- 废弃旧"按实体类型固定基准角"逻辑（这是同类型节点堆叠同一射线的根因）；
- 改为全圈层均匀扇形展开：起始角 `-90°-99°`，跨度 `min(1.15π, max(0.42π, n*0.3))`，节点按角度均分；
- 类型错位角（组织 0 / 人物 11° / 国家 22°）仅用于微调相邻类型间距，不再产生同角堆叠；
- 新增**跨圈层极角分离**：不同圈层节点极角差 <0.21rad（≈12°）时，外圈节点旋转让位，避免不同圈层节点落在同一条射线上（如 AQIM 与 IS Sahel 曾同为 171.7°，现已分离为 173.9° / 163.3°）。

## 3. 命中区域扩大与层级（CSS + JS）

### 3.1 透明点击命中层

- 每条关系线由 `.graph-edge-group` 分组承载两条线：
  - `.graph-edge-hit`：透明（`stroke:rgba(20,80,126,0)`）、`stroke-width:16px`（窄屏 14px）的宽命中层，承载 click/keydown；
  - `.graph-edge`：视觉线（2px，保持原视觉宽度），`pointer-events:none`；
- 视觉线宽不变，但可点击区域由 2px 扩大至 16px；命中层覆盖整条线段（含箭头区域），鼠标落在关系线附近即可触发。

### 3.2 标签与箭头避让

- 关系标签：从中点正上方改为**垂直于线段偏移 12px**，避开箭头头部区域；标签组 `pointer-events:none`；
- 节点标题：固定于节点下方（外围节点 y=56，中心 y=78），`pointer-events:none`；
- 箭头终点附近保留留白（edgePoint 已按节点半径截断线端点，标签不再进入箭头头部周边）。

### 3.3 层级与 pointer-events

- 图层顺序：`edgeHitLayer（命中层）→ edgeLayer（视觉线）→ labelLayer（关系标签）→ nodeLayer（节点）`；
- `.graph-edge{pointer-events:none}`、`.graph-edge-label-wrap{pointer-events:none}`、`.node-label{pointer-events:none}`、`.graph-edge-labels{pointer-events:none}`、`.graph-nodes{pointer-events:none}` 且 `.graph-node{pointer-events:auto}`；
- 文本层不再阻断关系线点击；节点组仍可点击切换中心；
- 选中样式：`.graph-edge-group.selected` 同步加粗视觉线并着色箭头。

## 4. 修正前后截图对比

`qa-artifacts-i1b/`：

| 场景 | 修正前 | 修正后 |
| --- | --- | --- |
| JNIM 中心（桌面） | `before-jnim.png` | `after-jnim.png` |
| JNIM 中心（390px） | `before-jnim-390.png` | `after-jnim-390.png` |

修正前基线：I1-A 最终提交 `8cb62cc` 的 `network.js`/`intelligence.css`（本地 8783 端口渲染），修正后为当前构建（8782 端口）。

## 5. 浏览器测试结果（Edge/Chromium 151 + CDP）

### 布局量化

```text
JNIM 中心（默认重点视图，6 节点）：
  最小节点中心距：100px（AQIM—IS Sahel）
  小于 90px 的近距对：0
  同圈层同极角：0（sameRay=[]）
  跨圈层同射线：已消除（AQIM 173.9° vs IS Sahel 163.3°）

圈层分布：
  内圈：基地组织 184px/135.6°、AQIM 179px/173.9°、马里 179px/-113.3°
  中圈：IS Sahel 270px/163.3°
  外圈：伊亚德·阿格·加利 347px/-170°
  （内圈 3 节点均匀分布 135.6°/173.9°/-113.3°，不再挤在一条线上）

其他中心切换后的最小节点距：
  IS Sahel 中心：424px    Al-Qaida 中心：248px
  Iyad 中心：180px        Mali 中心：100px
```

### 命中与点击

```text
视觉线宽：2px；命中层宽：16px（8 倍可点击区域）
命中层长度：75px（覆盖整条线段）
偏移 5px 点击（视觉线外）：成功打开关系详情面板
```

### 标签与箭头

```text
节点标题/关系标签与关系线段距离 <10px 的重叠：0
（修正前旧基线为 7 处 bbox 相交；修正后按线段距离实测 0 处）
```

### 响应式与错误

```text
1366×768：bodyWidth=1351，无横向溢出，7 条命中层
390×844：bodyWidth=390，无横向溢出，7 条命中层
控制台错误：0    网络失败请求：0
单页加载无异常（快速导航产生的 unhandled rejection 为旧页 fetch 打断的导航伪影，已在 I1-A 报告说明）
```

### 自动化回归

```text
test_demo_data.py / test_demo_pages.py / test_demo_v02.py：全部 PASS
构建：12 entity routes + 20 relation routes + network + data（PASS）
```

## 6. 结论

两个问题均已解决：

1. **"节点过近"**：同圈层节点最小中心距 100px（规格要求 90–110px），无 <90px 近距对；圈层半径自适应扩大，标签与关系线实测 0 重叠；
2. **"同线堆叠"**：同圈层节点角度均匀展开（内圈 3 节点 3 个独立极角），跨圈层同射线通过角度让位消除；视觉上明确呈现"并列围绕中心"而非"串联一列"；
3. 附带修复：关系线/箭头可点击区域由 2px 扩大至 16px 透明命中层，偏移点击验证通过；文本层不再阻断点击。

## 7. 任务边界确认

未增加实体/关系、未修改关系事实、未重做视觉风格、未新增功能（无地图/时间轴等）、未修改主站其他内容；仅改动 `network.js`、`intelligence.css` 与 QA 脚本/证据。
