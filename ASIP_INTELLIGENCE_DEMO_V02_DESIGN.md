# ASIP安全情报微型样板 V0.2 产品设计方案

- **基线**：V0.1归档标签 `asip-intelligence-demo-v0.1`，解引用提交 `320c6d6b2a1c4edbfbb40d62667d83c25ad5d057`
- **开发基线**：远端恢复分支提交 `356e58e76a9b93f46ca1bf8b189b270c0d197dea`
- **开发分支**：`feature/asip-intelligence-demo-v02`
- **工作区**：`C:/Users/kenan/WorkBuddy/recovery/asip-intelligence-v02-clean`
- **冻结范围**：保留12个实体、20条关系、V0.1路由和原生SVG/JS技术栈；不增加实体，不增加地图/时间回放/新闻自动关联/AI实时调用，不加入ASIP正式导航。

## 1. V0.1产品审计问题清单

Stage 0 使用真实Chrome/130 CDP检查入口、JNIM关系图、JNIM档案，以及已有V0.1真实浏览器证据覆盖的IS Sahel、人物和国家中心/档案状态。确认的主要问题：

1. 关系图外围节点主要按圆周均匀分布，关系语义分区不够明确。
2. 中心节点虽然尺寸更大，但缺少更强的层级背景、焦点徽标和结构化副标题。
3. 关系标签容易在中心附近形成拥挤，长标签对阅读帮助有限。
4. 组织、人物、国家已有形状区别，但节点文字层级和类型标识仍可更产品化。
5. 关系线已区分普通、敌对和历史状态，但领导、组成、活动存在等语义还可进一步显式化。
6. 右侧信息卡已有基础实体信息，但缺少“档案等级、核验状态、关系分组摘要”等正式产品信息。
7. 搜索、过滤、缩放和图例在桌面端集中在同一工具带，窄屏下需要更清晰的分组与折叠策略。
8. 档案正文目前以共享简版结构为主，组织、人物、国家缺乏明确的类型化栏目。
9. 档案等级和资料完整度尚未成为显式产品字段。
10. 来源、可信度、最后核验时间存在，但缺少统一的“证据与核验”区块。
11. 资料不足的实体需要明确显示L1基础档案，不应呈现为空白的“完整档案”。
12. 入口和页面已有技术样板说明，应通过版式、信息层级、状态徽章和栏目结构降低技术样板感。

审计结论：无需迁移前端框架或重写V0.1；使用原生SVG、CSS和JavaScript即可在12实体/20关系规模内完成V0.2优化。

## 2. V0.2关系图布局规则

### 2.1 语义分区

由关系类型驱动展示分组，不改变事实数据：

- `affiliated_with`、`led_by`、`founded_by`、`part_of_network`：上方/上右“体系与领导”区。
- `constituent_of`：左侧“组成/下属”区。
- `historically_associated_with`：左下“历史关联”区。
- `hostile_to`：下方“敌对/冲突”区。
- `operates_in`：外圈或下右“活动/存在”区。
- 其他关系：按实体类型和剩余空间放入“其他关系”区。

当中心为人物或国家时仍使用同一映射；分区只由关系类型、实体类型和当前画布空间计算，不针对JNIM写死坐标。

### 2.2 布局算法

1. 中心实体固定在画布视觉中心。
2. 邻接实体先按展示分组，再按实体类型排序。
3. 每个分组具有相对固定的角度带和半径带。
4. 同一分组内使用弧段均匀排列，并执行最小间距约束。
5. 关系标签采用短显示名，详细说明放在右侧关系卡。
6. 共有节点优先继承旧中心下的相对方向，再向新分区目标位置缓动。
7. 空间不足时压缩弧段和增大半径，不改变关系顺序。
8. 节点超过当前画布可读阈值时优先保持点击区域和类型区分，允许标签省略。

## 3. 节点视觉规则

- 组织：圆形，中心组织使用双层轮廓、内环和“组织/组”标识；外围组织较小。
- 人物：圆角矩形，使用“人物/人”标识；不使用头像。
- 国家：稳定方形或六边形，使用“国家/国”标识。
- 中心节点同时使用尺寸、边框、阴影/背景、类型标识和粗体标签突出，不只依靠颜色。
- 普通、悬停、键盘焦点、已选中、争议/时间敏感状态分别有明确视觉。
- 长名称采用短标签+第二行英文/辅助文本或省略号，避免溢出。
- 所有节点保持足够点击区域，支持键盘Enter/Space。

## 4. 关系线视觉规则

- 组成/隶属：实线，带方向提示。
- 领导/创始：较粗实线，标签使用“领导/创始”。
- 关联/效忠：实线，方向和短标签结合。
- 敌对：高对比虚线或特殊端点，配合敌对标记。
- 活动/存在：低透明度细线，避免被误解为控制关系。
- 历史关联：虚线、较低透明度并附“历史”标识。
- 争议或时间敏感：线型和徽标结合，详情卡显示争议/核验状态。
- 线上的文字只显示短关系名；时间范围、当前状态、可信度和来源全部在关系详情卡展示。

## 5. 中心切换动画规则

- 保留`requestAnimationFrame`动画和URL `focus`同步。
- 默认动画约420ms，使用ease-out。
- 共有节点继承旧方位并向新分区目标位置移动。
- 无关节点淡出，新节点淡入，不整图随机重排。
- 连续点击时由当前状态重新计算目标，避免重复节点。
- `prefers-reduced-motion: reduce`时关闭或显著缩短动画。
- 浏览器前进/后退、右侧卡片、关系详情和URL继续同步。

## 6. 右侧信息卡结构

1. 当前实体类型和焦点徽标；
2. 标准中文名、英文名和稳定ID；
3. 当前状态、档案等级、可信度、最后核验日期；
4. 一句平台核心判断；
5. 直接关系总数及按展示分组的摘要；
6. 查看完整档案入口；
7. 关系详情卡：双方、关系类型、时间范围、当前状态、可信度、来源；
8. 争议/时间敏感提示；
9. 移动端下置于画布后，保持可读和可操作。

## 7. 实体档案模板

### 7.1 组织模板

标题与别名 → 类型/状态/档案等级/可信度/核验时间 → 平台核心判断 → 组织简介 → 形成背景与历史沿革 → 组织体系/组成/分支 → 主要领导层 → 活动国家与地区 → 目标或思想倾向 → 行动方式 → 合作/关联/敌对关系 → 代表性事件 → 当前态势 → 区域影响 → 关系图 → 相关实体 → 来源与核验 → 争议/资料缺口。

没有可靠来源的资金、资源或支持关系栏目不显示为事实。

### 7.2 人物模板

姓名与别名 → 状态/档案等级/可信度/核验时间 → 所属组织与职务 → 简介 → 生平与活动阶段 → 创建/领导/指挥关系 → 主要活动区域 → 代表性事件 → 当前影响 → 关系图 → 相关实体 → 来源与争议说明。

### 7.3 国家模板

国家基本安全概况 → 档案等级/可信度/核验时间 → 当前主要风险 → 活跃安全主体 → 安全力量概览 → 重点风险地区 → 跨境关系 → 代表性事件 → 形势演变 → 当前趋势判断 → 关系图 → 相关组织/人物 → 来源与核验。

### 7.4 地区未来模板

只定义规范，不批量生成：地理范围、涉及国家、活跃组织、人物、跨境路线、风险热点、历史演变、关系图、未来地图接口、当前态势、专题研判、来源。

## 8. L1/L2/L3标准

- **L1基础档案**：唯一ID、标准名/别名、类型、状态、简述、涉及国家/地区、主要关系、1—2个可靠来源、核验时间、可信度、资料缺口。
- **L2标准档案**：L1 + 形成背景、历史沿革、领导层/所属关系、组织体系或活动区域、行动方式、代表性事件、当前关系与态势、至少3个可靠来源、时间敏感标记。
- **L3深度档案**：L2 + 分阶段历史、领导变化、内部组成、重要人物、地理/活动变化、关系变化、战术演变、重大反恐行动、当前能力/趋势、区域影响、资料争议、至少4—5个高质量来源和结构化时间轴/关系数据。

## 9. V0.2示范档案等级

- L3组织：JNIM、IS Sahel。
- L2组织：AQIM。
- L2人物：Iyad Ag Ghali。
- L2国家：Mali。
- 其余7个现有实体：保持统一L1基础档案；不新增实体、不扩充正式库规模。

## 10. 明确不做事项

不迁移React/Vue，不引入图库，不新增地图、地图图层、时间回放、新闻自动采集、AI实时调用、数据库服务器、用户账户、在线编辑、正式导航入口、自动实体识别、悬停预览、全站反向链接或推荐算法；不修改V0.1标签，不修改main/master/gh-pages。

## 11. 页面验收方式

使用Chrome/130 CDP检查：JNIM、IS Sahel、Al-Qaida、Iyad Ag Ghali、Mali中心关系图；验证语义分区、中心层级、节点文字、关系详情、切换动画、历史、搜索、过滤、深层URL、1366×768和390px布局，以及控制台/网络错误。

检查JNIM/IS Sahel L3、AQIM/Iyad/Mali L2和至少两个L1页面，验证类型化模板、档案等级、相关实体、来源/核验、时间敏感提示、图谱双向跳转和深层刷新。每个Stage建立提交、测试和证据检查点。
