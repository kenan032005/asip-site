# Python
__pycache__/
*.pyc

# 构建产物（由 build_site.py 生成；gh-pages 由 Actions 管理，不提交）
dist/
.deploy_tmp/

# 密钥与本地配置（绝不入库）
*.token
.env
sync_config.json
tools/sync_config.json
secrets.*
*.secret

# 操作系统
.DS_Store
Thumbs.db
logs/
logs_collect.txt

# 临时探针文件（非仓库内容）
.tool_probe.tmp

# 注意：.github/workflows/ 必须入库，GitHub Actions 才能运行。
# 推送需带 workflow scope 的令牌（gh auth login 或具 workflow 权限的 PAT）。
# 本次收口仅纳入 asip-pipeline.yml；以下两个本地工作流非本阶段内容，暂不入库。
.github/workflows/auto-update.yml
.github/workflows/deploy.yml

# 运行锁（本地运行时状态，绝不入库）
data/.pipeline.lock
.dist_trash/
.dist_new/

# Stage2 本地自动备份与验证临时目录（不入库）
data/**/.backups/
data/.backups/
data/.trash_*/

data/backup/

# Stage 2.5A 内部 AI 目录：运行时内容绝不入库（保留 .gitkeep 占位空目录）
data/ai/
data/ai/**/*.json
data/ai/queue/*
data/ai/processing/*
data/ai/completed/*
data/ai/failed/*
data/ai/cache/*
data/ai/usage/*
# Stage 2.5B-1 内部目录：批次/租约/审计/锁运行内容绝不入库
data/ai/batches/*
data/ai/leases/*
data/ai/audit/*
data/ai/locks/*
data/ai/**/*.md
!data/ai/**/.gitkeep

# Stage 2.5B-2A 单会话手工交接演示运行时（绝不入库）
.workbuddy_runtime/

data/runtime/

# Stage 5 核实运行时（dry-run 结果等；绝不入库，可审计摘要另存 docs/）
data/verification/

# QA headless browser profile (never committed)
.qa-edge-profile/
qa-artifacts-depth-d/scratch/
qa-artifacts-depth-e/scratch/
qa-artifacts-depth-f/scratch/
# Depth G Edge headless QA profile
.edge-g-profile/
# Expansion A Edge headless QA profile (browser cache, never committed)
qa-artifacts-expansion-a/edge-profile/
# Expansion B Edge headless QA profile (browser cache, never committed)
qa-artifacts-expansion-b/edge-profile/
# UI/UX V2 Edge headless QA profile (browser cache, never committed)
qa-artifacts-uiux-v2/edge-profile/
# Expansion C Edge headless QA profile (browser cache, never committed)
qa-artifacts-expansion-c/edge-profile/
# Online preview Edge headless QA profile (browser cache, never committed)
qa-artifacts-online-preview/edge-profile/
# UI Final Polish Pack 1 Edge headless QA profile (browser cache, never committed)
qa-artifacts-ui-final-polish-1/edge-profile/
# UI Hard Fix A Edge headless QA profile (browser cache, never committed)
qa-artifacts-ui-hard-fix-a/edge-profile/
# Expansion D Edge headless QA profile (browser cache, never committed)
qa-artifacts-expansion-d/edge-profile/
# Expansion E Edge headless QA profile (browser cache, never committed)
qa-artifacts-expansion-e/edge-profile/
# Global Audit P1 Edge headless QA profile (browser cache, never committed)
qa-artifacts-post-expansion-global-audit-p1/edge-profile/
# Final Depth Consolidation A Edge headless QA profile
qa-artifacts-final-depth-consolidation-a/edge-profile/
# Post-Consolidation Global Audit P2 Edge headless QA profile
qa-artifacts-post-consolidation-global-audit-p2/edge-profile/

# V2 Preview 隔离构建产物与本地 QA 证据（绝不入库；数据 runtime 已由 data/runtime/ 忽略）
preview_dist*/
preview_dist_v2.new/
preview_dist_v2.trash/
.workbuddy_tmp/
outputs/

# 本地构建垃圾目录（不入库）
dist.trash/
